import org.gradle.api.tasks.diagnostics.DependencyReportTask

initscript {
    // repositories {
    //     mavenCentral()
    // }

    dependencies {
        // If the plugin was published instead of copied we would just have to apply it
        classpath files("%%CUSTOM_JAR%%")
        // delete veriffication-metadata.xml
        // classpath("com.google.guava:guava:32.1.3-jre")
    }
}

allprojects {
    apply plugin: CustomModelInjectionPlugin
//    task dependencyReport(type: DependencyReportTask) {
//        outputs.upToDateWhen { false }
//        outputFile = new File(buildDir, "dependencies.txt")
//    }
//    task htmlDependencyReport(type: HtmlDependencyReportTask) {
//        reports.html.outputLocation = buildDir
//    }
}